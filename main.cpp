#include <iostream>
#include "functions.h"

int main() {
    std::cout << checkPalindrome("qowoeq");
    return 0;
}
//int* ptr = null_ptr; this is better because you don't want a garbage value
//int **ptr is a pointer that points to another pointer which points to a value
